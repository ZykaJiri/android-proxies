<?xml version="1.0" encoding="utf-8"?>
<!-- No policies needed: device-owner reboot() does not require a uses-policy.
     This receiver exists only so the app can be provisioned as device owner. -->
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies />
</device-admin>
